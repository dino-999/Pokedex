CREATE TABLE IF NOT EXISTS pokemon (
                         id BIGINT PRIMARY KEY AUTO_INCREMENT,
                         name VARCHAR(255),
                         height INT,
                         weight INT,
                         type VARCHAR(255)
);

